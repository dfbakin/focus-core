# Docker-specific requirements.
# torch / torchvision / numpy come preinstalled in the vast.ai base image and are
# pinned via a pip constraints file in the Dockerfile, so they are NOT listed here.
# Installed WITH transitive deps (the Dockerfile no longer uses --no-deps).
# mediapipe pulls opencv-contrib-python; opencv-python-headless is also kept to
# mirror the local Poetry environment (cv2 resolves to whichever loads last).

pytorchvideo>=0.1.5
lightning>=2.4.0
hydra-core>=1.3.2,<2.0.0
mlflow>=3.0.0
torchmetrics>=1.0.0
opencv-python-headless>=4.10.0
mediapipe>=0.10.0
pandas>=2.0.0
scikit-learn>=1.4.0
tqdm>=4.60.0
matplotlib>=3.8.0
pillow>=10.0.0
rich>=13.0.0
pyyaml>=6.0.0
